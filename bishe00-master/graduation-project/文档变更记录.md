# 文档变更记录

## 说明

本文档记录了项目中现有文档与当前代码状态之间的差异。在开发过程中，代码可能经过多次修改和优化，但文档可能没有同步更新。本文档的目的是：

1. 标注哪些文档内容已过时或不准确
2. 说明当前的实际实现状态
3. 解释为什么会有这些变化
4. 保留原始文档供参考，不直接删除

---

## 零、重要发现：IDA MCP 未实现

### ⚠️ IDA MCP 功能未实现

**实际情况**：
- 代码中 **只有 JADX MCP 实现**（`internal/mcp/jadx.go` 等）
- 代码中 **没有 IDA MCP 的实现**
- `web/mcp.html` 页面标注为"占位区"
- `main.go` 注释提到 IDA 但实际没有实现

**相关文件**：

| 类型 | 文件 | 说明 |
|------|------|------|
| 已实现 | `internal/mcp/jadx.go` | JADX MCP 连接 |
| 已实现 | `internal/mcp/jadx_handlers.go` | JADX HTTP 处理器 |
| 已实现 | `internal/mcp/jadx_stream.go` | JADX 流式处理 |
| 已实现 | `web/jadx_mcp.html` | JADX 前端页面 |
| 占位 | `web/mcp.html` | 标注为"占位区" |
| 文档 | `shili/ai/IDA_MCP*.md` | 多篇调研文档 |

**文档中提到但未实现的功能**：

| 文档 | 内容 | 状态 |
|------|------|------|
| `IDA_MCP使用说明.md` | IDA MCP HTTP 模式配置 | ❌ 未实现 |
| `IDA_MCP实现思路.md` | IDA MCP 实现方案 | ❌ 未实现 |
| `IDA_MCP问题完整总结.md` | IDA MCP 问题分析 | ❌ 未实现 |
| `IDA_MCP完美解决方案.md` | IDA MCP 解决方案 | ❌ 未实现 |

**为什么不删除这些文档**：
- 记录了开发过程中的调研和尝试
- 未来可能参考这些思路实现
- 了解项目演进过程

**建议**：
- 答辩时如果被问到 IDA MCP，应如实说明"有调研但未最终实现"
- 或者主动说明"当前版本已实现 JADX MCP，IDA MCP 在后续版本规划中"

---

## 一、代码结构变更

### 1.1 主程序文件变化

**当前状态**：

| 文件 | 行数 | 说明 |
|------|------|------|
| `main.go` | 5641 行 | 核心程序，包含所有主要功能 |
| `ai_exp.go` | 20KB | AI EXP 生成功能 |
| `ai_exp_verify.go` | 34KB | AI EXP 自动验证 |
| `ai_exp_batch.go` | 6KB | 批量 AI EXP |
| `exp.go` | 19KB | EXP 执行引擎 |
| `waf.go` | 11KB | WAF 绕过 |
| `upload.go` | 3KB | 文件上传 |
| `internal/mcp/` | - | MCP 协议实现 |
| `internal/shouji/` | - | JS 资产收集 |

**变更原因**：代码经过多次迭代，功能模块化拆分更清晰，但文档可能是早期版本。

### 1.2 依赖变化

**当前 go.mod**：
```go
module bishe
go 1.21
require gopkg.in/yaml.v3 v3.0.1
```

**文档中可能提到**：需要多个第三方库（如果真有这类描述的话）

**变更原因**：项目保持最小依赖，只用了 `gopkg.in/yaml.v3`，其他功能都是标准库实现。这样部署更简单。

---

## 二、功能实现状态对照

### 2.1 端口扫描模块

**文档描述**：基本功能完整

**当前实现**（main.go:407）：
- ✅ TCP/UDP 端口扫描
- ✅ 自定义端口范围
- ✅ 并发控制（默认 500）
- ✅ 超时控制
- ✅ Banner 抓取
- ✅ SSE 实时推送

**变更说明**：文档描述与实际实现一致。

---

### 2.2 目录扫描模块

**文档描述**：基本功能完整

**当前实现**（main.go:523）：
- ✅ 多字典支持
- ✅ 并发扫描
- ✅ 状态码判定（200/301/302/403）
- ✅ 响应长度记录
- ✅ 自定义字典上传

**变更说明**：文档描述与实际实现一致。

---

### 2.3 POC 扫描模块

**文档描述**：支持 Legacy、X-Ray、Nuclei 三种格式

**当前实现**（main.go:1420）：
```go
func pocScanHandler(w http.ResponseWriter, r *http.Request) {
    var pocs []POC
    var xrps []XRPOC
    var nucs []NucleiPOC
    // 三种格式都支持
}
```

**变更说明**：文档描述与实际实现一致。

---

### 2.4 AI 分析模块

**文档描述**：支持 AI 扫描和报告生成

**当前实现**：
- ✅ `/ai/auto-scan` - 自动化扫描工作流
- ✅ `/ai/analyze` - 报告分析
- ✅ `/ai/exp/python` - AI 生成 EXP
- ✅ 多 AI 提供商支持（DeepSeek、OpenAI、Anthropic、Ollama）
- ✅ **结构化报告生成**（新功能）

**重要更新**：

2026-04-04 新增了结构化报告格式：

```
【一、执行摘要】
【二、端口开放情况】
【三、敏感目录泄露】
【四、漏洞详情】
【五、EXP代码】
【六、风险修复建议】
【七、整体安全建议】
```

---

### 2.5 MCP 集成模块

**⚠️ 部分实现**

**文档描述**：支持 IDA Pro 和 JADX

**当前实现**：
- ✅ JADX MCP - 已实现
  - 连接管理
  - 工具调用
  - 流式响应
  - 前端页面 `jadx_mcp.html`
- ❌ IDA MCP - **未实现**
  - `web/mcp.html` 只是占位页面
  - 没有 IDA 相关的 Go 代码

**建议**：答辩时主动说明这一情况。

---

### 2.6 JS 资产收集模块

**文档描述**：支持 JS 解包、URL 提取

**当前实现**（`internal/shouji/shouji.go`）：
- ✅ 目标可解包性判定
- ✅ JS 解包（调用外部工具）
- ✅ URL 提取
- ✅ 结果合并分类

**变更说明**：文档描述与实际实现一致。

---

### 2.7 WAF 绕过模块

**文档描述**：支持多种绕过策略

**当前实现**（`waf.go`）：
- ✅ 大小写变形
- ✅ URL 编码
- ✅ 路径填充
- ✅ 参数分割
- ✅ 无害头注入

**变更说明**：文档描述与实际实现一致。

---

## 三、文档与代码差异详情

### 3.1 文档：《功能实现总结.md》

**位置**：`docs/功能实现总结.md`

**内容状态**：⚠️ 部分过时

**过时内容**：
- 文档中提到的文件路径可能与当前不符
- 文档创建时描述的代码结构，后来有调整

**当前代码位置**：
- AI EXP 生成：`ai_exp.go`
- AI EXP 验证：`ai_exp_verify.go`
- 主程序：`main.go`

**建议**：以当前代码为准，该文档仅作为历史参考。

---

### 3.2 文档：《更新日志.md》

**位置**：`docs/更新日志.md`

**内容状态**：✅ 已更新

**已有记录**：
- v2.0 - AI EXP 自动验证功能
- v2.1 - AI 流程中断问题修复、验证逻辑增强
- v2.2 - AI 报告格式重写（新增）

**建议**：无需进一步更新。

---

### 3.3 文档：《毕业设计论文详细版》

**位置**：`graduation-project/毕业设计论文_详细版.md`

**内容状态**：⚠️ 需要修改

**需要修改的部分**：
- AI 报告部分 - 已添加新格式说明
- MCP 集成部分 - 需要说明"只有 JADX MCP，IDA MCP 未实现"

---

### 3.4 IDA MCP 相关文档

**位置**：`shili/ai/IDA_*.md`

**内容状态**：⚠️ 仅供参考

**说明**：
- 这些文档记录了开发过程中对 IDA MCP 的调研
- 包括使用说明、实现思路、问题总结等
- 但最终**没有实现** IDA MCP 功能
- 保留这些文档是为了解项目演进过程

**建议**：
- 答辩时被问到可以说明："有进行调研，但最终未实现"
- 或者："当前版本实现了 JADX MCP，IDA MCP 在后续版本中考虑"

---

### 3.5 文档：《对比.md》

**位置**：`shili/说明/对比.md`

**内容状态**：✅ 仍然准确

**核心观点**：
- Go 语言的并发优势
- 与 Java、PHP 的对比
- SSE 相比 WebSocket 的优势
- 单二进制部署的便利性

**说明**：这些技术选型的理由在今天仍然成立，文档无需修改。

---

### 3.6 文档：《指纹.md》

**位置**：`shili/说明/指纹.md`

**内容状态**：✅ 准确

**说明**：指纹库内容分析报告，基于 `library/finger.json` 编写，与当前指纹库内容一致。

---

## 四、待更新的文档

| 文档 | 当前位置 | 建议 |
|------|----------|------|
| 毕业设计论文详细版 | `graduation-project/` | 更新 MCP 部分说明（只有 JADX） |
| 功能实现总结 | `docs/` | 标注为历史参考 |

---

## 五、不删除旧文档的理由

以下文档虽然可能过时或部分不准确，但我们保留它们而不是删除，原因如下：

1. **历史参考价值**：记录了开发过程和思路演变
2. **可能有用的片段**：部分内容可能对未来维护或扩展有帮助
3. **对比参考**：了解早期设计思路与最终实现的差异
4. **调研记录**：如 IDA MCP 文档，记录了未实现的尝试

---

## 六、最新变更摘要（2026-04-04）

### 变更 1：AI 报告格式重写

**问题**：原有 AI 分析报告格式不够结构化，用户导出后阅读体验差。

**解决方案**：
- 后端重写 AI 提示词，生成包含 7 个部分的结构化报告
- 前端优化导出函数，将【】标题转换为 Markdown 格式

**影响文件**：
- `main.go`（AI 提示词部分）
- `web/ai_analysis.html`（导出函数部分）

### 变更 2：代码编译验证

**验证命令**：
```bash
cd bishe00-master && go build -o neonscan.exe .
```

**结果**：编译成功，无错误

### 变更 3：IDA MCP 确认未实现

**检查结果**：
- `internal/mcp/` 目录下只有 JADX 相关文件
- `web/mcp.html` 是占位页面
- `shili/ai/IDA_*.md` 是调研文档，但未实现

---

**最后更新**：2026-04-04
